class Solution {
public:
    int missingNumber(vector<int>& nums) {
   int n = nums.size();
   int Sum1, Sum2;
   Sum1 = (n*(n+1))/2;
   for(int i = 0;i<n-1;i++){
    Sum2+=nums[i];
   }
   int Number = Sum1-Sum2;
   return Number;
}
};
